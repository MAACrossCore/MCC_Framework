# 必须与 .github/workflows/install.yml 的 MAAFW_VERSION 对齐（去掉 v 前缀），
# 并与 MCCAvalonia 的 Maa.Framework.Runtimes 一致（当前 5.12.3），
# 否则 AgentClient/AgentServer 可能协议不兼容。
# 不要用 >=：线上 pip 会把绑定升到比原生/UI 更新的版本。
maafw==5.13.0
numpy
